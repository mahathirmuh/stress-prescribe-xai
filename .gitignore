# Environment
.venv/
venv/
env/
.env
.env.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Large model artifacts (>5 MB) — too big for git, regenerable from notebook
models/random_forest.pkl
models/random_forest.zip
data/processed/splits.pkl
data/processed/splits.zip
# Small models (catboost.cbm, tabnet.zip, encoders) tetap di-track untuk reproducibility

# CatBoost training logs (auto-generated)
# catboost_info/

# MS Office temporary lock files
~$*

# PDF exports — biasanya di-regenerate dari DOCX, tidak perlu track tiap iterasi
# Note: kk-ngebut_paper.pdf yang sudah committed sebelumnya tetap ter-track
# (gitignore tidak hapus file yang sudah di-track)
*.pdf

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Outputs (boleh di-track sebagian, tergantung kebutuhan)
# outputs/figures/*.png
# outputs/recommendations/*.json
